services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: jra
      POSTGRES_PASSWORD: jra_dev_password
      POSTGRES_DB: jra
    ports:
      - "5433:5432"
    volumes:
      - jra_pgdata:/var/lib/postgresql/data

volumes:
  jra_pgdata:
